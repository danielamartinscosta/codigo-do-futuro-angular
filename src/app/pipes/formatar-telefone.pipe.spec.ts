import { FormatarTelefonePipe } from './formatar-telefone.pipe';

describe('FormatarTelefonePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatarTelefonePipe();
    expect(pipe).toBeTruthy();
  });
});
